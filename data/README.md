# Data

Place the four ROOT simulation files used by the notebook in this directory.

The `.root` files are excluded from Git by the project `.gitignore`, so they will remain local when the repository is pushed to GitHub.

Expected files:

- `nominal.root`
- `detector_3cm.root`
- `detector_6cm.root`
- `detector_15cm.root`
